  ---
  - name: Display STC
    hosts: zos
    gather_facts: false 
    environment: "{{ environment_vars }}"

    tasks:

    - name: Display STC
      zos started tasks:
        state: "displayed"
        job name: "all" 

        